# Admin credentials (seeded on first run; changing these re-syncs the password).
ADMIN_EMAIL=admin@quantedge.local
ADMIN_PASSWORD=ChangeMeNow!123

# Secret used to hash submitter IPs (set a long random value in production).
IP_HASH_SECRET=change-me-to-a-long-random-string

# Optional: override storage locations (defaults live under web/data/).
# DB_PATH=./data/app.sqlite
# UPLOAD_ROOT=./data/uploads
