import AdminInboxPage from '../../views/AdminInboxPage'

export const metadata = {
  title: 'Admin inbox',
  robots: { index: false, follow: false },
}

export default function Page() {
  return <AdminInboxPage />
}
